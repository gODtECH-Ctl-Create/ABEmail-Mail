# Saved Signatures

Saved signatures are managed from `/signatures` for the authenticated mailbox.

A signature can be enabled or disabled and one signature per mailbox can be marked as the default. A default enabled signature is inserted into a genuinely empty Compose editor automatically.

The HTML version is stored for future rich-email support; the current plain-text composer uses the text version.