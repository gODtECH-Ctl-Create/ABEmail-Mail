# ABEmail UI references

The responsive mailbox workspace uses established interaction patterns from modern email clients while keeping ABEmail's own visual identity.

Reference categories:
- Outlook: desktop folders, message list, search, reading-pane behavior
- Gmail: simple inbox/search and message hierarchy
- Proton Mail: compact responsive workspace and composer behavior

The implementation is original ABEmail code and styling, not a copy of any referenced product.